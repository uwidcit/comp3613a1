{
  "inputs": [
    {
      "type": "promptString",
      "id": "render-api-key",
      "description": "Render API Key (Dashboard → Account Settings → API Keys). Do not commit the key.",
      "password": true
    }
  ],
  "servers": {
    "render": {
      "type": "http",
      "url": "https://mcp.render.com/mcp",
      "headers": {
        "Authorization": "Bearer ${input:render-api-key}"
      }
    }
  }
}
